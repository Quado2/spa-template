import * as React from 'react';

interface IHomeProps {
}

const Home: React.FunctionComponent<IHomeProps> = (props) => {
  return (<div>
    <h1> Top Component just to test</h1>
  </div>);
};

export default Home;
